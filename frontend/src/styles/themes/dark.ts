import { DefaultTheme } from 'styled-components';
import pattern from '.';

const dark: DefaultTheme = {
  colors: {
    background: '#99791E',
    text: '#f3f3f3',
  },
  ...pattern,
}

export default dark;